🧪 Add tests for pricing-sync validate function

🎯 **What:** The testing gap for the validate function in cloud/pricing-sync/lib/validate.js is now addressed.
📊 **Coverage:** The tests cover the happy path (valid data object), edge cases (missing or invalid top-level fields, non-serializable data, invalid repair fields, stats object validation), and error conditions (like a >20% repair count difference).
✨ **Result:** Test coverage for validate.js is now implemented using Node's built-in test runner, preventing future regressions and improving codebase reliability.
