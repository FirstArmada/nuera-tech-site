# Vercel deploys only the static site at the repo root.
# The Google Cloud server code under cloud/ is deployed to Cloud Run by its own
# workflow (.github/workflows/deploy-cloud.yml) and must not ship to Vercel.
cloud
tests

# Agent learnings notes — kept in-repo but never part of the deployed site (mirrors .assetsignore).
.jules

# Unreferenced root social-card assets (the live OG image is assets/icons/og-image.png).
social-card.png
social-card.svg
