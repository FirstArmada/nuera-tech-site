{
  "name": "nuera-pricing-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud Run Job: regenerate pricing-data.json from the Google Sheets master price list (+ authorized Mobile Klinik overlay), validate it, and open a GitHub PR. Auth via ADC; GitHub token from Secret Manager.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node job.js",
    "dry-run": "node job.js --dry-run"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^6.1.3",
    "@octokit/rest": "^22.0.1",
    "googleapis": "^173.0.0"
  },
  "overrides": {
    "uuid": "^14.0.0",
    "gaxios": "^7.1.5",
    "google-auth-library": "^10.7.0"
  }
}
