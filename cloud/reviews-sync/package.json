{
  "name": "nuera-reviews-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud Run Job: fetch the shop's Google reviews (Places Details API), regenerate reviews.json, validate it, and open a GitHub PR. GitHub token from Secret Manager; Places API key from Secret Manager or env.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node job.js",
    "dry-run": "node job.js --dry-run"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "@octokit/rest": "^21.0.2"
  }
}
